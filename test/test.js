let  = require('assert');

describe('Is a function', function() {
  describe('#typeOf()', function() {
    it('should return be a function', function() {
      assert.equal([1,2,3].indexOf(4), -1);
    });
  });
});